extends layout

block content
  if messages
    .messages.info
      span= messages
  h1= title
  p Welcome to #{title}
  if(!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if(user)
    p You are currently logged in as #{user.username}
    a(href="/logout") Logout
